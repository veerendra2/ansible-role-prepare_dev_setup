---
user_home: "{{ ansible_env.HOME }}"
user_name: "{{ ansible_env.USER }}"

pip_install_packages:
  - setuptools
  - jmespath
  - requests
  - thefuck
  - beautifulsoup4
  - youtube_dl
  - ansible
  - tldr
  - protonvpn-cli
