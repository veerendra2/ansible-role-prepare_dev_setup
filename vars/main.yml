---
user_home: "{{ ansible_env.HOME }}"
user_name: "{{ ansible_env.USER }}"

pip_install_packages:
  - ansible
  - beautifulsoup4
  - jmespath
  - loguru
  - requests
  - setuptools
  - thefuck
  - tldr
  - youtube_dl

arch_map:
  arm64: "aarch64"
  x86_64: "x86_64"
